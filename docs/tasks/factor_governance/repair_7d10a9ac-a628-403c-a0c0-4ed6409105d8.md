# P/G 因子生产修复验收记录

## 执行边界

- 正式修复 ID：`7d10a9ac-a628-403c-a0c0-4ed6409105d8`
- 因子运行 ID：`07d78ba7-6b6f-4880-9417-2531f283d508`
- 公式版本：P `v2.0`、G `v1.1`
- 修复窗口：2025-10-03 至 2026-09-11 的全部自然周五
- 执行时间：2026-09-14 15:31:40 至 16:23:02（Asia/Shanghai）
- 状态：`success`

本次只执行 P/G 修复，没有执行或修复 PIT，也没有注册 Windows 计划任务。

## 失败关闭与回滚演练

第一次执行 `10371317-15f4-4092-b733-c90381fda878` 在计算
2026-02-20 的 G 时失败关闭。`301501.SZ` 的三个 P 同比差在数据库六位精度下
完全相同，但二进制浮点标准差为约 `3.55e-15`，使
`g_efficiency_surprise` 放大到约 `-4.29e15`，超过数据库字段精度。

自动回滚恢复了全部已触及快照：P 的 79 个日期记录、G 的 50 个日期记录，合计
129 个日期记录均为 `rolled_back`。回滚后行数、日期数、29 个非周五日期以及日期
摘要校验和与修复前基线完全一致：

| 表 | 行数 | 日期数 | 非周五行 | 日期摘要校验和 |
| --- | ---: | ---: | ---: | --- |
| `factors.p_factor` | 3,707,530 | 1,390 | 149,685 | `3655b863ad880bc96c9aa1cd4d9b9b56` |
| `factors.g_factor` | 3,677,522 | 1,248 | 149,681 | `a6df720b02706b12ec53a27352a93050` |

修复将 P 差值先按既有六位数据库精度规范化；只有存在真实离散度时才使用标准差，
否则沿用 G v1.1 原有的未归一化差值回退。该改动没有改变公式权重、参数或版本号，
并已加入浮点噪声黄金样本测试。

## 正式修复结果

| 动作 | 日期数 | 归档旧行 | 写入新行 |
| --- | ---: | ---: | ---: |
| 删除 P 非周五快照 | 29 | 149,685 | 0 |
| 删除 G 非周五快照 | 29 | 149,681 | 0 |
| P 周五影子差异替换 | 50 | 167,866 | 271,842 |
| G 合法周五全量重算 | 50 | 167,864 | 271,842 |

50 个修复窗口日期中，18 个原本缺失、32 个替换既有快照。归档表按本次
`repair_id` 保存 P 317,551 行、G 317,545 行，可由统一回滚入口恢复。

2000-05-05 至 2000-08-04 的 14 个早期 P 周五经资格查询确认无合格 PIT 输入，
逐日记录为 `expected_no_data`，没有写入伪造空快照。

最终表状态：

| 表 | 行数 | 日期数 | 首日 | 最新日 | 非周五行 |
| --- | ---: | ---: | --- | --- | ---: |
| `factors.p_factor` | 3,661,821 | 1,379 | 2000-01-07 | 2026-09-11 | 0 |
| `factors.g_factor` | 3,631,819 | 1,237 | 2003-01-03 | 2026-09-11 | 0 |

P/G 的“必须是自然周五”约束均已验证，`convalidated=true`。

## 独立验收

`FactorRepairService.acceptance('2026-09-11')` 返回 `passed=true`，阻断项为空：

- 周五缺口均为 0；P 的 14 个无数据日期均有 `expected_no_data` 证据。
- 非周五、空主键、重复主键、`ann_date > calc_date`、分数越界均为 0。
- G 结果集合与既有 G 资格集合的日期级差异为 0。
- `pgs_factors` 兼容视图与 `factors` 基表行数差均为 0；P/G 日期摘要校验和分别为
  `5672fdad62c91372ff11949ec25d65bf` 和 `97ec8fb980d11a9c5de7078848627b90`，
  视图与基表一致。
- P/G 审计状态均为 `healthy`，最新日覆盖率均为 1.0；公共 `task_status` 的最近
  执行状态均为 `success`，执行时间与审计时间分别记录。
- 修复后的 `smart` 预检状态为 `ready`，P/G 计划日期总数为 0，首次正式来源水位
  已建立。

## 代码与运行检查

- `python -m pytest tests/unit -q`：748 passed，1 warning。
- `python -m pytest tests/integration/test_factor_governance_pipeline.py -q`：
  1 passed，1 skipped；生产关系所有权检查通过，需专用
  `ALPHAHOME_FACTOR_TEST_DATABASE_URL` 的破坏性隔离库用例按设计跳过。
- 本次变更 Python 文件的 Ruff 检查通过；相关目录 `compileall` 通过；
  `git diff --check` 通过。
- 周任务安装器默认预览输出正确，系统查询确认
  `AlphaHome-Factor-Weekly` 不存在，未执行 `-Apply`。
