# FTTM 一级、二级行业 PIT 实施与验收记录

> 实施日期：2026-08-25（Asia/Shanghai）
> 实施状态：Validate 通过（数据合同、代码、2014 年以来全历史回填与审计快照）
> 边界：不构成行业 FTTM 因子有效性、样本外有效性或生产投资授权

## 实施版本

- 个股公式：`fttm_q4_report_event_linear_v1`
- 行业聚合：`org_mv_weighted_then_equal_mean_v1`
- 质量规则：`coverage_gate_v1`
- 权重口径：`total_mv`（研究代理，不是申万官方指数权重）

## 注册任务

- `pit_stock_fttm_monthly` → `pit.pit_stock_fttm_monthly`
- `pit_industry_fttm_monthly` → `pit.pit_industry_fttm_monthly`

行业任务声明依赖 `pit_stock_fttm_monthly`、`pit_industry_classification`。统一更新器从 `PITTaskContract.dependencies` 展开传递依赖、分拓扑层执行并传播上游失败状态。

## 实现确认

- FY1/FY2 只在同一 `(ts_code, org_name, author_name, report_date)` 事件内配对。
- `np` 优先；仅允许 `report_date == trade_date` 的 `eps × total_share` 回退。
- 六个月窗口为左开右闭；先计算事件 FTTM，再选股票—机构月末最新事件。
- 一级、二级行业均使用同月申万动态成分和 PIT-time 上市/退市状态。
- 行业内先按机构实际覆盖股票重新归一化 `total_mv`，再对机构值做算术平均。
- 环比与扩散度只连接立即前一个自然月；无匹配机构时扩散度为空。
- 当最早写入月缺少已持久化的前月个股锚点时，从原始研报只读补算该锚点并仅用于本批计算，不把锚点越界写入目标表。
- 低覆盖行业保留结构行，并以稳定 JSON 原因数组标记质量。
- 重算月通过 staging 校验后在同一事务中删除并重写，避免源行删除后遗留旧数据。

## HANDOFF DDL 差异

`org_mv_weighted_then_equal_mean_v1` 长 34 个字符，而 HANDOFF 建议的 `aggregation_version varchar(32)` 无法保存该固定版本名。正式 DDL 将该列扩为 `varchar(64)`；版本值和聚合语义没有改变。首次发现时行业写入事务完整回滚，修正列宽后重跑成功。

## 运行证据

本次实际重放区间：`2014-01-31` 至 `2026-07-31`，共 151 个完整自然月。这里的“全历史”采用 HANDOFF V1 固定默认起点 2014-01-31；不是把源表可追溯到的 2010–2013 年自动扩入 V1。

| 输出 | 行数 | 月数 | 起止 `obs_date` |
| --- | ---: | ---: | --- |
| `pit.pit_stock_fttm_monthly` | 2,760,303 | 151 | 2014-01-31 至 2026-07-31 |
| `pit.pit_industry_fttm_monthly` | 23,977 | 151 | 2014-01-31 至 2026-07-31 |

2025-12-31 锚点修复验证：

| 层级 | 结构行业 | `matched_org_count` | `up_org_count` | 无可比机构行业 |
| --- | ---: | ---: | ---: | ---: |
| L1 | 31 | 1,525 | 636 | 0 |
| L2 | 131 | 4,538 | 1,841 | 2 |

因此原先 `up_org_count` 全为 0 不是“全部机构均未上调”，而是 2025-11-30 锚点未进入计算。修复后两层汇总均非 0；L2 仍有 2 个行业确实没有相邻月可比机构，按合同保留 `matched_org_count=0`、`diffusion_up=NULL`。

2026-07-31 行业截面：

| 层级 | 结构行业 | 有产值 | eligible |
| --- | ---: | ---: | ---: |
| L1 | 31 | 31 | 29 |
| L2 | 131 | 129 | 103 |

个股最新月审计：18,413 行、2,885 只股票、96 家机构；双年度配对率 97.8493%，单边回退率 2.1507%，未来来源行 0，主键重复 0。

## 验收结果

- PIT/FTTM 相关单元回归：94 passed。
- 刷新链路补充回归：8 passed。
- 全量单元回归：519 passed、2 failed；两项失败均来自既有 `test_tinysoft_default_start_dates.py`，原因是 `tinysoft_stock_margin` / `tinysoft_stock_margindetail` 尚未进入该测试的 FULL 起始日期审阅表，与本次 FTTM 与 GUI 加载路径无关。
- 真实 AlphaDB 只读 pipeline smoke：1 passed。
- 151 个月落库后 SQL 验收：个股主键重复 0、行业主键重复 0、未来数据泄漏 0、公式不一致 0、行业扩散度不一致 0、月份缺口 0、L1/L2 结构缺口 0。
- 审计分母：个股使用审计月当时有效股票；行业使用同月申万 L1/L2 结构行业。行业审计不引用伪造 `ts_code`。
- 2026-08-25 22:09（Asia/Shanghai）已持久化两项正式审计快照，状态均为 `healthy`；22:12 已写入两项 `success` 执行记录，供界面展示最近运行时间。

## GUI 全历史加载性能

全历史回填后，PIT 管理页原实现会对每个任务串行聚合全部历史期间的覆盖数。当前 8 个 PIT 任务的真实加载耗时为 19.8393 秒，其中实时表统计占 19.7964 秒；特征列表自身仅需 0.0674 秒，但启动时被串行排在 PIT 之后。

2026-08-25 完成以下修复：

- 覆盖率从“全历史一次性分组”改为“从最新候选期向前逐期检查”，保留最新有效期与低覆盖回退语义。
- 8 个 PIT 任务的实时统计并发执行；当前上市股票分母通过异步锁只加载一次。
- GUI 启动时并发请求数据采集、PIT、特征和设置，特征列表不再等待 PIT。
- 控制器复用桌面入口已建立的数据库连接池，不再启动后立即关闭并重建。
- 精确总行数查询耗时较小且属于可信展示字段，因此继续保留，没有改成近似统计。

修复后同一 AlphaDB 上 PIT 列表加载为 0.7248 秒，较修复前减少约 96.3%；特征列表为 0.0649 秒。8 个任务的新旧覆盖期和覆盖数逐项一致，差异 0。

## 已知限制

- 源表没有在本任务中采用经过验证的行级变更日志；日常增量保证最近 8 个完整月及相邻月传播，但不能声称自动捕获任意久远的历史补录或删除。
- 2010–2013 年虽有部分源数据，但不在 HANDOFF V1 默认历史范围内；若要扩展到 2014 年以前，仍应先输出年度覆盖率剖面，再显式变更数据合同并重跑验收。
- 月末快照只声明收盘后可生成，最早在 `obs_date` 后首个交易日使用；日级 `report_date` 不支持盘中可见性声明。

## 2026-08-26：相邻月修正分解生产落库

本轮没有改写 `fttm_q4_report_event_linear_v1` 或既有聚合历史。只新增非破坏性的共同样本分解，用于避免把 `fttm_np_mom_rate`、`diffusion_up` 直接解释为分析师盈利上修。

真实 AlphaDB 对照显示，2026-07-31 全A旧口径 `fttm_np_mom_rate=10.44%`、`diffusion_up=88.30%`；在同股票、同机构、同 FY1/FY2 目标年、双年度预测完整，并用本月共同股票和权重重算后：

- `revision_rate=0.20%`：预测值变化；
- `horizon_roll_rate=0.29%`：FY1/FY2 权重自然滚动；
- `revision_activity_rate=14.08%`：共同样本中本月实际发生预测变化的股票占比；
- `revision_up_stock_rate=57.54%`：只在发生变化的股票中计算上修占比；
- `revision_comparable_weight_rate=85.12%`：共同可比分解覆盖的全A权重。

因此，对 2026-07-31 全 A 截面而言，旧环比的大部分变化来自独立快照的股票/机构覆盖集合和机构内归一化变化，不是纯预测修正。旧字段继续保留为兼容与诊断字段；新增字段使用 `common_stock_org_decomp_v1`，并单独输出 `is_revision_eligible`、`revision_quality_reasons`。

生产迁移和全历史回填于 2026-08-26 完成：

- 行业表新增 12 个修正分解字段和 4 个比例范围约束；回填 2014-01-31 至 2026-07-31 共 151 个月、23,977 行，`revision_rate` 非空 23,569 行，修正质量 eligible 17,598 行。
- 指数/全 A 表采用相同字段与约束；回填同一历史范围共 1,674 行，`revision_rate` 非空 1,674 行，修正质量 eligible 1,509 行。
- 两表空主键、重复主键、月份缺口、比例越界、PIT 日期越界、修正资格合同异常均为 0；正式 PIT 审计均为 `healthy`、覆盖率 100%、`gap_count=0`。
- 回填前后既有 FTTM、环比和扩散核心字段的全表校验哈希分别保持不变：行业 `12dad1743146321309b00cebc6f1b26a`，指数/全 A `348cebddb2f21f0e6a3b8e788590f3ca`。
- 对 2014-01-31、2020-06-30、2026-07-31 三个时点独立复算并逐字段比对，行业 475 行、指数/全 A 31 行与落库结果一致。
- 23:31（Asia/Shanghai）已写入两项 `success` 执行记录，GUI 最近运行时间与本次全量回填同步。
- 机器可读验收记录见 [fttm_revision_backfill_validation_20260826.json](./fttm_revision_backfill_validation_20260826.json)。

这里的“生产”仅表示 PIT 数据任务、字段合同和历史数据已经落库；修正指标的因子有效性、策略有效性和投资授权仍未验证。
