# Tinysoft 智能更新失败排查与恢复（2026-09-06）

## 范围与原始记录

本次仅处理 2026-09-05 晚间智能更新中状态为 `error` 的两个任务：

| 任务 | 失败时间（Asia/Shanghai） | 原状态详情 |
|---|---|---|
| `tinysoft_stock_hsgt_top10` | 2026-09-05 22:06:18 | 处理完成 (行数: 0) |
| `tinysoft_fund_stock_holding_detail` | 2026-09-05 23:03:22 | 处理完成 (行数: 0) |

上一轮两个任务均成功。原始任务异常被 GUI 结果摘要覆盖，现有数据库状态不足以区分源站超时、限流或其他错误；相应时段 PostgreSQL 日志没有这两张表的错误记录。不能据此认定某一类网络错误为根因。

## 已完成检查与修正

- 按当前真实配置（Tinysoft OPI）读取 smart 窗口；十大成交 4 个通道、1 批，基金持仓 15,057 个基金主代码、31 批。
- 十大成交接口抽查返回 880 行；基金持仓首批返回 25,111 行；均通过原有数据校验，字符串长度未超出对应字段定义。
- `task_execution_service.run_tasks` 对返回 `status="error"` 的任务优先保存 `error` / `message`，以 ERROR 级别显示原始异常；没有详情时也明确显示“执行失败”，不再写“处理完成”。
- 验证命令：`python -X utf8 -m pytest tests/unit/test_task_execution_service.py -q`，退出码 0，9 项通过。
- 提交前审查补充 `error` 优先于 `message`、空错误字段回退、无详情提示和 ERROR 日志级别验证；该文件现有 13 项测试，已随全项目 673 项单元测试全部通过。
- smart 默认走非流式路径；本次没有修改流式分支、任务日期窗口、基金代码范围、数据源优先级或字段口径。

## 复跑证据

通过 GUI 使用的 `run_tasks` 服务按原配置执行智能更新。完整日志：`tmp/tinysoft_smart_verification_20260906.log`；状态和覆盖摘要：`tmp/tinysoft_smart_verification_20260906.json`。

- 十大成交：2026-09-06 09:38:22 成功处理 880 行，四个通道最新日期均为 2026-09-04。相同数据的 UPSERT 不改变已有行的 `update_time`，因此业务表写入水位仍可早于本次任务完成时间。
- 基金股票持仓：2026-09-06 09:57:42 成功处理 796,876 行，全部 4 项校验通过；库内总行数由 9,449,927 增至 9,449,995（净新增 68 行），最新报告期仍为 2026-06-30。保存事务已成功提交。
- 两个任务均按原 smart 配置完整执行成功，复跑脚本退出码为 0；此次没有复现原始任务异常。历史失败的直接原因仍无法从旧摘要还原，不将本次恢复等同于已证明某一种故障原因被永久消除。
