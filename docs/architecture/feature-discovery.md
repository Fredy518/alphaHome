# Features 注册表发现

`FeatureRegistry.discover(force_reload=True)` 重新扫描模块声明，包括已经被 Python 缓存的模块。先构建并校验候选注册表，所有模块成功后才发布；导入错误、无效声明或重复名称保留此前的完整注册表。外部扩展注册也会保留。

该选项不会重新执行缓存模块的顶层代码。修改配方源代码后应在新的进程中加载版本，避免运行中的类和依赖被部分替换。

发现过程与注册过程分开加锁；等待模块导入时不持有注册锁。其他线程可以完成合法注册，发现发布时合并其外部扩展。读取者看到完整的已发布注册表。
