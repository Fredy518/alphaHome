# NAS 第三阶段：Y 盘整库备份与恢复门槛（2026-09-25）

> 本文记录备份完成时的决策状态。随后用户明确选择以同一 NAS 不同硬盘上的 Y 盘归档作为本次回退点，并要求继续生产迁移；实际执行与尚未验证的恢复边界见 [第四阶段记录](nas-stage4-production-migration-20260925.md)。

## 本次结果

用户指定此后 NAS 备份放在 Y 盘。`Y:` 映射到 `\\Dh4300plus-e81c\娱乐`，该主机解析为 NAS 数据库所在的 `192.168.5.6`。本次从 `192.168.5.6:15432/alphadb` 只读导出；没有对生产数据库执行 DDL、DML、刷新或迁移。

| 项目 | 结果 |
| --- | --- |
| 数据库 | PostgreSQL 17.11；备份前 `pg_database_size` 为 130,828,965,555 字节；数据库为 UTF8、ICU `zh-CN`，当前非系统角色只有 `postgres`。 |
| 完整逻辑归档 | `Y:\AlphaHomeBackups\nas-alphadb-full-20260925.dir`；`pg_dump` directory 格式、6 个并行工作进程、zstd level 3；2026-09-25 18:01:36 至 18:27:53（北京时间）成功完成。281 个文件，共 22,299,648,053 字节。 |
| 内容哈希 | `Y:\AlphaHomeBackups\nas-alphadb-full-20260925.manifest.json` 对 281 个归档文件逐一记录文件名、长度和 SHA-256；清单自身 SHA-256 为 `c9a5ffe22657f693d857054d0cb980df60b1e3ffb908fc94121000fa653f4e0d`。 |
| 目录与结构检查 | `pg_restore --list` 成功列出 2,597 个条目，含 280 个 `TABLE DATA` 和 33 个 `MATERIALIZED VIEW DATA` 条目；结构 SQL 可由归档生成。TOC 和结构 SQL 分别存于同目录，SHA-256 分别为 `897803f6262a85a3dc4b8bef5e11ca4ac7a7c359863d1b7dbb3255199bee6ef0` 和 `56f8fa5cc9ced6a3e816d9645a13dba85cee3e7f78494395c47b8bc9a6ba262b`。 |
| 全局角色清单 | `nas-globals-no-passwords-20260925.sql`，使用 `pg_dumpall --globals-only --no-role-passwords --no-tablespaces`；SHA-256 为 `263b533fea856d34b9660d8c966dcfb2a74b7b85d5db44298e0041998652255d`。不含口令或口令哈希。 |

第一次单进程尝试因读取速度慢主动停止，`Y:\AlphaHomeBackups\nas-alphadb-full-20260925.dump.partial`（1,752,047,616 字节）是**无效、不完整**的归档，不能用于恢复。`zstd-capability-test-20260925.dump` 仅为结构级能力测试，也不能充当整库备份。两者与上述成功完成的目录归档须严格区分。

## 此备份能证明什么

备份命令成功退出、目录清单可读、结构可提取，且完成了逐文件哈希。这证明文件已完整写入并形成可复核的固定内容，但**没有证明整库能成功恢复**。Y 盘与数据库处于同一台 NAS，适合在 NAS 硬件和共享均正常的前提下作为此次数据库迁移的回退点；它不是跨设备灾难恢复副本。Y 盘共享的实际用户可读范围尚未完成独立审查，归档包含生产数据，应限制访问。

## 进入生产写入前的下一门槛

用户选择在 NAS 上使用**独立 PostgreSQL 实例**验证完整恢复。该实例应使用独立端口与数据目录，有足够容纳约 131 GB 数据及恢复期 WAL/索引的空间，兼容 PostgreSQL 17 与源数据库的 UTF8/ICU 配置，不接入生产调度。不能把生产实例中新建一个数据库称为独立实例。

将 Y 盘归档完整恢复到该实例后，必须核对恢复命令零错误、关键表精确行数与业务键、索引/约束、视图、owner/ACL 和应用只读计划。当前只有生产库连接与 Y 盘共享，NAS SSH 22 端口不可用；独立实例的管理入口/连接信息尚待取得，因此恢复验收未完成。长期维护 NAS 的电脑是当前关机的 3900X，不是 SSH 配置中的 `maxp395`；正式发布前还要复核 3900X 的实际 `main` 提交和任务停写状态。

在恢复验收、停写核对与生产迁移计划复核完成前，不执行第二阶段的 NAS 结构或历史数据写入，也不把恢复分支推到远端 `main`。第二阶段隔离演练见 [NAS 第二阶段演练记录](nas-stage2-rehearsal-20260925.md)。
