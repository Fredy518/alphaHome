"""
AlphaHome 统一命令行界面（CLI）

本模块提供统一的命令行入口点，整合所有生产能力、工具和工具集。

核心设计：
- 顶层命令：`ah`，支持多个子命令组
- 统一的参数契约：--log-level, --format, --config
- 统一的退出码和异常处理
- 向后兼容：保留旧入口点不变

子命令组：
- ddb：DolphinDB 数据库工具
- mv：物化视图管理
- prod：生产脚本和任务
- gui：GUI 应用（可选）
"""

__version__ = "1.0"
